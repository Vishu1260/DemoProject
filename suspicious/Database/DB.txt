CREATE TABLE  "ALERTLIST" 
   (	"MAILTO" VARCHAR2(40) NOT NULL ENABLE, 
	"MAILFROM" VARCHAR2(40) NOT NULL ENABLE, 
	"SUBJECT" VARCHAR2(40) NOT NULL ENABLE, 
	"MESSAGE" VARCHAR2(400)
   )
CREATE TABLE  "ALERTWORDS" 
   (	"ALERTWORDS" VARCHAR2(40) NOT NULL ENABLE
   )
CREATE TABLE  "BLOCKLIST" 
   (	"MAILTO" VARCHAR2(40) NOT NULL ENABLE, 
	"MAILFROM" VARCHAR2(40) NOT NULL ENABLE, 
	"SUBJECT" VARCHAR2(40) NOT NULL ENABLE, 
	"MESSAGE" VARCHAR2(400)
   )
CREATE TABLE  "ENCRYPTEDWORDS" 
   (	"AWORDS" VARCHAR2(400) NOT NULL ENABLE, 
	"EWORDS" VARCHAR2(400) NOT NULL ENABLE
   )
CREATE TABLE  "INBOX" 
   (	"MAILTO" VARCHAR2(40) NOT NULL ENABLE, 
	"MAILFROM" VARCHAR2(40) NOT NULL ENABLE, 
	"SUBJECT" VARCHAR2(4000) NOT NULL ENABLE, 
	"MESSAGE" VARCHAR2(4000)
   )
CREATE TABLE  "LOGIN" 
   (	"USERID" VARCHAR2(40) NOT NULL ENABLE, 
	"PASS" VARCHAR2(40) NOT NULL ENABLE, 
	"ROLE" VARCHAR2(40) NOT NULL ENABLE
   )
CREATE TABLE  "MAILS" 
   (	"MAILTO" VARCHAR2(40) NOT NULL ENABLE, 
	"MAILFROM" VARCHAR2(40) NOT NULL ENABLE, 
	"SUBJECT" VARCHAR2(40) NOT NULL ENABLE, 
	"MESSAGE" VARCHAR2(4000) NOT NULL ENABLE
   )
CREATE TABLE  "SWORDS" 
   (	"WORDS" VARCHAR2(40) NOT NULL ENABLE
   )
CREATE TABLE  "USERINFO" 
   (	"SECRETQUESTION" VARCHAR2(400) NOT NULL ENABLE, 
	"SECRETANSWER" VARCHAR2(400) NOT NULL ENABLE, 
	"FIRSTNAME" VARCHAR2(40) NOT NULL ENABLE, 
	"MIDDLENAME" VARCHAR2(40), 
	"LASTNAME" VARCHAR2(40) NOT NULL ENABLE, 
	"DOB" VARCHAR2(40) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(40) NOT NULL ENABLE
   )
